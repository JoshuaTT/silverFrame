<Page
    x:Class="SilverFrame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SilverFrame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel>
                <TextBox Name="NewBlogUrl"></TextBox>
                <Button Click="Add_Click">Add</Button>
                <ListView Name="Pictures">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Url}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>


        <Button Content="Show" HorizontalAlignment="Center" Margin="0,250,0,0" VerticalAlignment="Top" Width="75" Click="Show_Click"/>
        <Button Content="Add" HorizontalAlignment="Center" Margin="0,300,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Info" HorizontalAlignment="Center" Margin="0,350,0,0" VerticalAlignment="Top" Width="75" Click="Info_Click"/>

    </Grid>
</Page>
