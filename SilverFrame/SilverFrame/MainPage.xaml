<Page
    x:Class="SilverFrame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SilverFrame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" MinWidth="600" MinHeight="600">
            <StackPanel>
                <TextBox Name="NewBlogUrl"></TextBox>
                <Button Click="Add_Click">Add</Button>
                <ListView Name="Pictures">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Url}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>


        <Button Content="Show" HorizontalAlignment="Stretch" Margin="475,250,474,0" VerticalAlignment="Top" Click="Show_Click" Height="32"/>
        <Button Content="Add" HorizontalAlignment="Stretch" Margin="475,300,474,0" VerticalAlignment="Top" Height="32"/>
        <Button Content="Info" HorizontalAlignment="Stretch" Margin="475,350,474,0" VerticalAlignment="Top" Click="Info_Click" Height="32"/>

    </Grid>
</Page>
